# CLAUDE.md

See [`AGENTS.md`](AGENTS.md) — the single source of truth for this repo's
governance (scope decisions, gate mechanics, standing rules, brand rules).

This file exists only so Claude-specific tooling that auto-discovers
`CLAUDE.md` by filename picks up project context. Do not add substantive
content here — edit `AGENTS.md` instead, and keep this pointer in sync if
`AGENTS.md` ever moves.
