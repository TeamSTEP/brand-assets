---
"@teamstep/design-system": minor
---

Introduce the primitives tier (`IconButton`, `Card`) and relocate `Cta` from `src/ui/` to `src/primitives/`. Migrate duplicated card surfaces and icon-only buttons across feature components onto the new primitives.
